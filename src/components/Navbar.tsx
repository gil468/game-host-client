import { Typography } from '@mui/material';

const Navbar = () => {
  return (
    <Typography sx={{ marginBottom: '10px' }} variant={'h4'}>
      Music Master
    </Typography>
  );
};

export default Navbar;
